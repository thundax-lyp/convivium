interface:
  display_name: "Convivium Push PR"
  short_description: "Push branch work and close out a PR."
  default_prompt: "Use $convivium-push-pr to push this branch and create or update the PR."

policy:
  allow_implicit_invocation: false
