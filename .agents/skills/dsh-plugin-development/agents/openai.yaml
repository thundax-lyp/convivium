interface:
  display_name: "DSH Plugin Development"
  short_description: "按当前 DSH API 和插件契约开发、验证 Convivium 插件"
  default_prompt: "Use $dsh-plugin-development for DSH plugin implementation, API verification, build, test, or runtime validation."
policy:
  allow_implicit_invocation: true
